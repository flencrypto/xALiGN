# Development / test-only dependencies.
# Install with: pip install -r requirements-dev.txt
# These are NOT installed into the production Docker image.
-r requirements.txt

pytest==8.2.2
pytest-asyncio==0.23.7
